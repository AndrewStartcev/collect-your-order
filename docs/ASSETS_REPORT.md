# Batch 01 / 01.1 — графика для проверки стиля

## Состав поставки

Batch 01 с доработкой строго по `DESIGN_REVIEW_BATCH_01.md`: 4 элемента окружения, 3 спрайта персонажа/тележки, 9 элементов UI и 12 товаров. Batch 01.1 заменяет только `order-panel.png` и `phone-frame.png`, добавляет `warehouse-backdrop.png` 1600×900 и обновляет сборочный preview. Production-файлы находятся в `assets/environment`, `assets/character`, `assets/ui`, `assets/products`.

`preview-batch-01.png` в корне проекта — сборочная композиция 1600×900 по `WIREFRAME.md`. `assets/contact-sheet-batch-01.png` — обзор отдельных элементов. Это материалы проверки, их не нужно нарезать для игры.

## Использование

- Координаты левой панели: (24,126), размер 350×750. Светлая cream-поверхность с тонкой navy-рамкой и маленьким зажимом. Безопасная область: x=16..334, y=36..730 относительно ассета.
- Телефон: (1216,126), размер 360×750. Тонкий navy-корпус без бронированных боков. Безопасная область контента: x=18..342, y=34..722 относительно ассета.
- Все надписи, числа, отзывы, строки заказа и прогресс должны выводиться игрой поверх PNG. В preview текст заказа тёмный на светлой бумаге; подложка отзыва приглушена, чтобы телефон меньше конкурировал с товарами. Координаты элементов сохранены.
- `warehouse-backdrop.png` — отдельный непрозрачный фон 1600×900, размещается первым слоем. Дальние коробки и лампы служат атмосферой, кликабельных товаров и текста в фоне нет.
- Кнопки — одинаковый canvas 300×72, текст размещается отдельно по центру. Три состояния поставляются отдельными файлами.
- Товар — canvas 64×64, центрирование по видимому силуэту, максимальная сторона 52–58 px. Не увеличивать разные товары по отдельным произвольным коэффициентам.
- Worker idle/pick — canvas 96×128. Тележка 160×128 поставляется пустой; товары в ней можно размещать отдельными иконками.
- Стеллаж 224×448 содержит четыре пустых уровня. В preview пять модулей размещены в зоне x=420..1170, y=190..680 с демонстрационным масштабированием. Production-интеграция должна сохранить сетку и hit areas утверждённого prototype.
- Рекомендуется nearest-neighbor для масштабирования растров; прозрачный alpha сохраняется. Фон пола является сплошной текстурой.

## Производство и воспроизводимость

Графика создана встроенным ImageGen, отдельным запросом на каждый ассет/состояние. Запросы и параметры экспорта перечислены в `assets/batch-01-manifest.json`. Крупные генерации остаются вне Git; в репозитории только финальные PNG.

`tools/art/normalize.py` приводит размер PNG к манифесту и исключает практически невидимый alpha-мусор из границ спрайтов. `tools/art/build-preview.py` собирает обзор из настоящих production-файлов. Подписи, простые подложки и значения в preview — демонстрационные элементы программного UI, не дополнительные production-ассеты.

`tools/art/verify.py` проверяет комплектность, размеры, формат RGBA, поля товаров, стык пола и размер preview. Результаты записываются в `assets/batch-01-qa.json`.

В исходном Batch 01 технический контроль составлял 27/27. После Batch 01.1 проверяются 28 PNG, включая новый непрозрачный backdrop; актуальный результат — в `assets/batch-01-qa.json`. Новые рамки экспортированы с отступом 3 px. Принятые 25 production PNG, включая товары, кнопки, иконки, пол, стеллажи, персонажа и тележку, остаются без изменений. Ранее выполненный отражённый экспорт пола также сохранён.

## Граница приёмки

Эта поставка — графика и сборочный preview. Код игры, механики, сохранения, SDK и layout не изменяются. Проверка в живом Phaser на 1600×900 и 1366×768 остаётся этапом интеграции и визуальной приёмки; статический preview её не заменяет.

Batch 02 не начат. Для перехода необходимо подтверждение руководителя после проверки Batch 01.
